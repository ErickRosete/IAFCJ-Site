const styles = theme => ({
  textfield: {
    margin: theme.spacing.unit
  },
  wrapper: {
    margin: "auto"
  },
  editor: {
    border: "1px solid lightgray",
    minHeight: "50vh"
  },
  button: {
    margin: theme.spacing.unit
  }
});

export default styles;
